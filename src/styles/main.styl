@import 'cozy-ui/defaults'
@import 'cozy-ui'
:global
    @import './search-bar'

div:focus
    outline: none

.fil-content
    top-padded(30px)

    @media (max-width: (768/basefont)rem)
        padding 0
