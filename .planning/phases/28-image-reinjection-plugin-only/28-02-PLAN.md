---
phase: 28-image-reinjection-plugin-only
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified: []
autonomous: false
requirements: [IMG-01, IMG-02, IMG-03, IMG-04, IMG-05]

must_haves:
  truths:
    - "Replace of a cell selection containing image(s) → a single undo restores the initial state."
    - "Replace → no perceptible flicker (one render pass)."
    - "Replace → final selection covers injected content (text + images), not a collapsed cursor."
    - "Image attributes preserved at save: size + crop/rotation; floating wrap preserved (or the documented anchor→PasteHtml fallback is enabled)."
    - "Paragraph-image case has the same guarantees (shared path)."
    - "Regression: existing image goldens (T9 cell, C1 ¶) and the Insert path stay green."
    - "getLocalImagePath fires and returns in the real cross-origin Cozy plugin frame (not only oo-dev)."
  artifacts:
    - path: ".planning/phases/28-image-reinjection-plugin-only/28-02-SUMMARY.md"
      provides: "recorded live-UAT results for Q1-Q4 + IMG-01..05 + regression, with pass/fail per check and any documented fallback"
  key_links:
    - from: "oo-dev container"
      to: "cozy-drive-image-reinject worktree"
      via: "./scripts/oo-dev-setup.sh re-point (shared container — coordinate)"
      pattern: "oo-dev-setup"
    - from: "forcesave .docx"
      to: "word/media/imageN.png present + <a:blip r:embed> resolves"
      via: "unzip/inspect the saved docx"
      pattern: "word/media"
---

<objective>
Live-verify the plugin-only image re-injection landed by Plan 28-01. The spike proved the round-trip on one inline cell image at forcesave; it could NOT run the save-time checks the RESEARCH flagged as the four open questions (Q1 floating, Q2 crop+rotation, Q3 cross-origin real Cozy, Q4 `ret.path` vs `ret.url`), nor the human-perceptible observables (single undo, no flicker, selection covers content). The jest golden harness is broken in this worktree (symlinked node_modules, per MEMORY), so the regression goldens are re-run live too.

Purpose: confirm IMG-01..05 hold end-to-end at save on a running editor and that the change does not regress existing image handling or the Insert path.
Output: `28-02-SUMMARY.md` recording pass/fail per check; if Q1 (floating) fails, document enabling the dormant `drawingType === "anchor"` PasteHtml fallback.

Note: **Ben drives these live UATs himself and reports** (project convention). Claude prepares the environment and diagnoses; it does not pilot the browser by default. All tasks are checkpoints — this plan is `autonomous: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-image-reinjection-plugin-only/28-CONTEXT.md
@.planning/phases/28-image-reinjection-plugin-only/28-RESEARCH.md
@.planning/phases/28-image-reinjection-plugin-only/28-01-SUMMARY.md

<env_setup>
oo-dev is a **shared** container. Before any live check, re-point it at this worktree and hard-reload the plugin:
- Run `./scripts/oo-dev-setup.sh` from `cozy-drive-image-reinject` (coordinate — do not clobber another worktree's session).
- Base branch already includes the fix L#2 (`558a10df2`) and the dev cache fix (`2abc46881`), so fresh `code.js` is picked up on hard reload (no-store + SW patch).
- Test docs: `table-image.docx` (cell image — spike doc), a paragraph-image doc (C1), and a doc containing a **floating/wrapped** image and a **cropped+rotated** image for Q1/Q2.
</env_setup>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Core observables — single undo, no flicker, selection covers content (IMG-01/02/03/05)</name>
  <files>(verification only — no production files modified)</files>
  <read_first>
    - .planning/phases/28-image-reinjection-plugin-only/28-01-SUMMARY.md — what landed, any ambiguous consumption site to exercise.
    - .planning/phases/28-image-reinjection-plugin-only/28-CONTEXT.md — the 4 reproduced bugs (flicker, lost selection, multiple undos) and their expected fixes.
  </read_first>
  <action>Re-point oo-dev at this worktree (`./scripts/oo-dev-setup.sh`), hard-reload the editor, then present the how-to-verify steps to Ben and record his reported results per check in 28-02-SUMMARY.md. Do not pilot the browser unless asked.</action>
  <what-built>Plan 28-01 replaced the marker+PasteHtml image re-injection with a single-callCommand FromJSON+AddDrawing path for cell and paragraph images.</what-built>
  <how-to-verify>
    Env: run `./scripts/oo-dev-setup.sh` from this worktree; hard-reload the editor.
    1. Open `table-image.docx`. Select a cell region containing **2 images + text** (Ben's repro case). Trigger a Scribe **Replace**.
       - IMG-02: watch for flicker — expect ONE render pass (no sequential per-image repaints).
       - IMG-03: after Replace, the selection must COVER the injected text + images (not a collapsed cursor).
       - IMG-01: press Ctrl+Z ONCE — expect the whole reply (text + both images) to revert to the initial state in a single undo (no leftover raw ` IMG:N ` marker, no second undo needed).
    2. Repeat on a **paragraph-image** doc (C1 case) — IMG-05: same three guarantees via the shared path.
  </how-to-verify>
  <verify>
    <human-check>IMG-01 single undo, IMG-02 no flicker, IMG-03 selection covers content — confirmed live for cell AND paragraph.</human-check>
  </verify>
  <acceptance_criteria>
    - IMG-01: exactly one Ctrl+Z restores the pre-Replace state for both cell and paragraph cases; no intermediate marker text appears.
    - IMG-02: no perceptible flicker / no sequential image repaints during Replace.
    - IMG-03: post-Replace selection spans the injected content (text + images), not a collapsed cursor.
    - IMG-05: paragraph-image case passes IMG-01/02/03 identically.
    - Result (pass/fail per check) recorded in 28-02-SUMMARY.md.
  </acceptance_criteria>
  <done>IMG-01/02/03/05 confirmed live for cell and paragraph; results recorded.</done>
  <resume-signal>Type "approved" if all four hold for both cell and paragraph, or describe the failing observable(s).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Fidelity + edge checks at save — Q1 floating, Q2 crop/rotation, Q4 ret.path, Q3 cross-origin (IMG-04)</name>
  <files>(verification only — no production files modified, except a possible one-line blip-value or fallback-flag flip if a check fails)</files>
  <read_first>
    - .planning/phases/28-image-reinjection-plugin-only/28-RESEARCH.md — "Open Questions" Q1-Q4; "Floating-image detection + fallback decision"; Risk 2 attribute table; Risk 4 (ret.path vs ret.url).
    - .planning/phases/28-image-reinjection-plugin-only/28-01-SUMMARY.md — whether `ret.path` or `ret.url` was used for the blip rewrite; where the dormant anchor fallback flag lives.
  </read_first>
  <action>Prepare test docs (inline, cropped+rotated, floating) in oo-dev, run the forcesave + saved-docx inspection steps with Ben, and record outcomes. If Q4 fails, switch the blip value `ret.path`→`ret.url` (one line) and re-test; if Q1 fails, flip the dormant floating-fallback flag so `drawingType === "anchor"` images route to PasteHtml, and document the loss. Any such one-line change is recorded in 28-02-SUMMARY.md.</action>
  <what-built>The FromJSON path restores full drawing fidelity (crop/rotation/wrap/effects) and registers media via getLocalImagePath before save; a dormant anchor→PasteHtml fallback hook exists.</what-built>
  <how-to-verify>
    1. **Q4 / non-orphan media (IMG-04 core):** Replace a selection with an inline image; **forcesave**; unzip the `.docx` and confirm `word/media/imageN.png` is present and the `<a:blip r:embed="...">` resolves (media NOT orphaned). If the image is blank/dropped, the rewrite value is wrong → switch the blip value from `ret.path` to `ret.url` (one-line change, documented in 28-RESEARCH.md) and re-test — record which value works.
    2. **Q2 crop+rotation (IMG-04):** Replace with a **cropped + rotated** image; forcesave; reopen and inspect the saved XML — confirm `graphic.blipFill.srcRect` (crop) and `spPr.xfrm.rot` (rotation) survive.
    3. **Q1 floating (IMG-04):** Replace a selection containing a **floating/wrapped** image; forcesave; confirm the saved doc keeps the anchor + wrap type (image NOT re-inlined). If the anchor is dropped/misplaced → enable the dormant fallback: flip the floating-fallback flag so `drawingType === "anchor"` images route to PasteHtml (documented loss per IMG-04 "or documented fallback"); re-test and record.
    4. **Q3 cross-origin (real Cozy):** In the REAL Cozy plugin frame (not oo-dev), trigger a Replace-with-image and confirm `executeMethod("getLocalImagePath", …)` fires its callback (image registers + saves). Record whether real-Cozy matches oo-dev.
  </how-to-verify>
  <verify>
    <human-check>Q4 media non-orphan at save; Q2 crop+rotation survive; Q1 floating wrap preserved (or fallback enabled + documented); Q3 cross-origin callback fires.</human-check>
  </verify>
  <acceptance_criteria>
    - Q4: after forcesave, `word/media/imageN.png` exists and `<a:blip r:embed>` resolves; the working blip value (`ret.path` or `ret.url`) is recorded.
    - Q2: `srcRect` (crop) and `xfrm.rot` (rotation) present in saved XML.
    - Q1: floating image retains wrap/anchor at save; OR the anchor→PasteHtml fallback is enabled and the documented-loss path is recorded.
    - Q3: getLocalImagePath callback fires in the real cross-origin Cozy frame.
    - All results recorded in 28-02-SUMMARY.md with the blip value and fallback state used.
  </acceptance_criteria>
  <done>IMG-04 confirmed at save (size+crop+rotation), floating outcome resolved (preserved or documented fallback), Q3 cross-origin confirmed, blip value recorded.</done>
  <resume-signal>Type "approved" with the confirmed blip value and floating outcome, or describe the failing check.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Regression — existing image goldens (T9 cell, C1 ¶) + Insert path stay green</name>
  <files>(verification only — no production files modified)</files>
  <read_first>
    - .planning/phases/28-image-reinjection-plugin-only/28-RESEARCH.md — regression note (Insert path untouched; re-run T9/C1 goldens).
    - .planning/phases/28-image-reinjection-plugin-only/28-01-SUMMARY.md — confirmation that the Insert path was not modified.
  </read_first>
  <action>Reproduce the T9, C1, and Insert-with-image scenarios live in oo-dev with Ben (jest is broken in this worktree), and record pass/fail. Flag any deviation as a gap for `/gsd-plan-phase 28 --gaps`.</action>
  <what-built>Only the Replace image path changed; the Insert path (original media stays live) was not modified.</what-built>
  <how-to-verify>
    Note: the jest golden harness is broken in this worktree (symlinked node_modules per MEMORY) — verify live in oo-dev.
    1. **T9 (cell image golden):** reproduce the T9 scenario (cell-image Replace) live; confirm the image survives Replace + save (non-orphan) and matches the golden expectation.
    2. **C1 (¶-image golden):** reproduce the C1 scenario (paragraph-image Replace) live; same confirmation.
    3. **Insert path:** perform an **Insert** (not Replace) of content containing an image; confirm it behaves exactly as before (original media stays live, image saves) — no regression from the Replace refactor.
  </how-to-verify>
  <verify>
    <human-check>T9 cell-image Replace, C1 ¶-image Replace, and Insert-with-image all behave as before (image saves, non-orphan).</human-check>
  </verify>
  <acceptance_criteria>
    - T9 cell-image Replace: image present + non-orphan at save (matches golden behavior).
    - C1 ¶-image Replace: image present + non-orphan at save (matches golden behavior).
    - Insert-with-image: unchanged behavior, image saves correctly (no regression).
    - Results recorded in 28-02-SUMMARY.md; any deviation flagged as a gap for follow-up.
  </acceptance_criteria>
  <done>T9, C1, and Insert confirmed regression-clean live; results recorded.</done>
  <resume-signal>Type "approved" if T9, C1, and Insert all pass, or describe the regression.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This is a verification-only plan — it modifies no production code and introduces no new trust boundary beyond those assessed in 28-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | Tampering | npm/pip/cargo installs | accept (N/A) | No package installs; live UAT only. |

**Assessment:** No production-code change (beyond a possible one-line blip-value/fallback flip), no dependency, no auth/network/secret surface. **No HIGH-severity threats.** Inherits the minimal-surface assessment of 28-01 (the getLocalImagePath error case T-28-01 is mitigated there).
</threat_model>

<verification>
- Every check runs against a real oo-dev editor (and, for Q3, the real Cozy frame) with a forcesave + saved-`.docx` inspection where fidelity is asserted.
- Automated harness verification is intentionally MISSING — jest is broken in this worktree and the checks are inherently save-time/perceptual; live human verification is the gate (Ben drives, per convention).
- If any check fails, it is recorded as a gap and closed via `/gsd-plan-phase 28 --gaps` (e.g. enabling the anchor fallback, or switching `ret.path`→`ret.url`).
</verification>

<success_criteria>
- IMG-01..05 confirmed live for cell and paragraph (single undo, no flicker, selection covers content, fidelity at save, shared-path parity).
- Q1-Q4 resolved with recorded outcomes (working blip value; floating preserved or fallback enabled; cross-origin confirmed).
- T9, C1, and Insert regression-clean.
- 28-02-SUMMARY.md records pass/fail per check and any documented fallback.
</success_criteria>

<output>
Create `.planning/phases/28-image-reinjection-plugin-only/28-02-SUMMARY.md` when done. Record per-check pass/fail (IMG-01..05, Q1-Q4, T9/C1/Insert), the confirmed blip rewrite value, and the floating-fallback state (dormant vs enabled).
</output>
